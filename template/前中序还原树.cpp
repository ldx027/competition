#include <iostream>
#include <vector>
using namespace std;

int main()
{
    int N;
    cin >> N;
    vector<int> front(N);
    vector<int> mid(N);

    for (int i = 0; i < N; i++) cin >> front[i];
    for (int i = 0; i < N; i++) cin >> mid[i];


}