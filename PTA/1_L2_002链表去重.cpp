#include <iostream>
#include <vector>
using namespace std;

struct Node
{
    int adrs;
    int val;
    int next;
};

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);

    int L, N;
    cin >> L >> N;
    vector<Node> mem(1e6 + 10);
}