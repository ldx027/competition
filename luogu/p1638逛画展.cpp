#include <iostream>
#include <vector>
using namespace std;

vector<int> worker;
vector<int> vec;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);

    int n, m;
    cin >> n >> m;

    worker.resize(n);
    vec.resize(m, 0);

}
