#include <iostream>
#include <vector>
#include <utility>
#include <deque>
using namespace std;

#define ll long long
#define inf 0x7f7f7f7f

int main()
{
    int N, L, R;
    cin >> N >> L >> R;

    vector<int> vec(N + 1);
    vector<int> dp(N + 1);
}